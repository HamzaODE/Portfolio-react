import PropTypes from 'prop-types'
import React, { Component } from 'react'

export class Nav extends Component {
    static propTypes = {}

    render() {
        return (
            <div>Nav</div>
        )
    }
}

export default Nav